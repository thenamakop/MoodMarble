# MoodMarble E2E Fix Plan

## Summary

Implement the confirmed native-startup fix by removing the unused `@expo/ui` dependency from `apps/mobile/package.json`, refreshing the lockfile from the repo root, rebuilding fresh Detox Android APKs, and re-running the targeted mobile regression checks. Preserve all current product code and all out-of-scope files. Use the current worktree as the baseline, which means `apps/mobile/detox.config.cjs` already satisfies the required `reversePorts: [8081, 8097]` state and should not be edited again unless its content drifts before execution starts.

## Current State Analysis

- `apps/mobile/src` contains zero `@expo/ui` imports. This confirms the package is currently orphaned at the application source level.
- `apps/mobile/package.json` still declares `"@expo/ui": "0.2.0-canary-20260121-a63c0dd"` in `dependencies`, so Expo autolinking can still register the native UI module during Android startup.
- `pnpm-lock.yaml` still contains `@expo/ui@0.2.0-canary-20260121-a63c0dd` and `expo-modules-core@3.0.30` resolution entries, so a root install is required to fully remove the package from the dependency graph.
- `apps/mobile/detox.config.cjs` already contains:
  - `build: "node ./scripts/detox-build-android.cjs"`
  - `reversePorts: [8081, 8097]`
  - `avdName: process.env.DETOX_AVD_NAME ?? "Pixel_10"`
- The repo is already dirty. Relevant existing changes include:
  - `apps/mobile/detox.config.cjs`
  - `apps/mobile/scripts/detox-build-android.cjs`
- `apps/mobile/scripts/detox-build-android.cjs` currently does **not** perform the `subst M:` short-path mapping described in the prompt. Per user direction, this script drift remains unchanged and is treated as known risk only.
- `apps/mobile/android/app/build/outputs/apk` does not currently exist, so the stale-APK cleanup step is expected to be a no-op unless execution creates new outputs first.
- The target E2E files exist:
  - `apps/mobile/e2e/member-journey.e2e.cjs`
  - `apps/mobile/e2e/manager-journey.e2e.cjs`
- The target regression tests exist:
  - `apps/mobile/src/features/onboarding/onboarding-screen.test.tsx`
  - `apps/mobile/src/features/mood-submission/marble-tray-screen.test.tsx`
  - `apps/mobile/src/features/history/history-screen.test.tsx`
  - `apps/mobile/src/features/dashboard/manager-route.test.tsx`

## Proposed Changes

### 1. Re-verify orphan dependency status

Files checked:
- `apps/mobile/src/**/*.ts`
- `apps/mobile/src/**/*.tsx`

What:
- Re-run the source grep for `@expo/ui` immediately before any edit.

Why:
- This is the safety gate before dependency removal.

How:
- If the grep still returns zero files, continue.
- If any source file references `@expo/ui`, stop execution and report the filenames instead of removing the package.

### 2. Remove the unused dependency declaration

File to edit:
- `apps/mobile/package.json`

What:
- Delete only the `@expo/ui` dependency entry from the `dependencies` object.

Why:
- This prevents Expo autolinking from registering `ExpoUIModule` in the generated native app.

How:
- Make no other dependency or script changes in this file.
- Preserve JSON formatting and all other versions exactly as-is.

### 3. Preserve Detox config as already compliant

File to preserve:
- `apps/mobile/detox.config.cjs`

What:
- Do not edit this file during execution unless it no longer matches the current required state.

Why:
- The requested `reversePorts: [8081, 8097]` entry is already present, so re-editing would create unnecessary churn against an already-dirty file.

How:
- During execution, quickly confirm the file still contains the required `android.debug` entry with the existing `build`, `reversePorts`, and `Pixel_10` lines.
- If unchanged, treat Action 3 from the prompt as already satisfied by the current baseline.

### 4. Refresh dependencies from the repo root

Files expected to change:
- `pnpm-lock.yaml`
- possibly workspace install metadata under `node_modules` only

What:
- Run `pnpm install` from `C:\MoodMarble`.

Why:
- This removes `@expo/ui` from the installed graph and updates `pnpm-lock.yaml` to match `apps/mobile/package.json`.

How:
- Execute from the repo root, not `apps/mobile`.
- If install fails, stop and report the concrete failure.
- Peer-dependency chatter tied to removing `@expo/ui` is informational unless it causes a non-zero exit.

### 5. Remove stale APK outputs

Path to clean:
- `apps/mobile/android/app/build/outputs/apk`

What:
- Delete the APK output directory before rebuilding.

Why:
- Detox must consume artifacts built after the dependency removal, not anything compiled with the old autolinked package set.

How:
- If the directory exists, remove it recursively.
- If it does not exist, record that the cleanup was a no-op and continue.

### 6. Rebuild Detox Android artifacts

Execution entry:
- `apps/mobile/scripts/detox-build-android.cjs` via `pnpm e2e:android:build`

What:
- Build fresh debug and AndroidTest APKs from `apps/mobile`.

Why:
- This regenerates the native Android project and APKs without `@expo/ui` in the dependency graph.

How:
- Run `pnpm e2e:android:build` from `C:\MoodMarble\apps\mobile`.
- Expect the resulting files:
  - `apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk`
  - `apps/mobile/android/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk`
- If the build fails, stop and report only the first non-warning error line.
- Do not edit `apps/mobile/scripts/detox-build-android.cjs`; if Windows path-length issues occur because the current script no longer uses `subst`, document that as the blocking cause.

### 7. Perform optional APK sanity check

Artifact to inspect:
- `apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk`

What:
- If `aapt2` is available on `PATH`, inspect the rebuilt APK for `expo.modules.ui`.

Why:
- This is a fast sanity check that the unwanted native module is gone from the rebuilt app.

How:
- Run the badging check only if `aapt2` resolves successfully.
- If the check shows `expo.modules.ui`, stop and report that prebuild/build did not pick up the dependency removal.
- If `aapt2` is unavailable, skip this check and note the skip explicitly.

### 8. Provide exact manual multi-terminal E2E sequence

Runtime components:
- backend dev server
- Metro bundler
- Detox test runner

What:
- Hand off the precise three-terminal sequence for the user to run manually.

Why:
- The user explicitly wants the final E2E execution coordinated across separate terminals and an already-booted emulator.

How:
- In the execution handoff, provide exactly:
  - Terminal 1: `cd C:\MoodMarble` then `pnpm --filter backend dev`
  - Terminal 2: `cd C:\MoodMarble\apps\mobile` then `pnpm e2e:android:metro`
  - Terminal 3: `cd C:\MoodMarble\apps\mobile` then `pnpm e2e:android:test`
- Note the readiness gates:
  - backend must be listening first
  - Metro must be ready second
  - emulator `Pixel_10` must already be booted to the home screen
- If Detox later fails on an element lookup instead of a crash, classify it as a product-level test issue and report the failing assertion line without changing test logic.

### 9. Run the targeted regression suite

Tests to run:
- `src/features/onboarding/onboarding-screen.test.tsx`
- `src/features/mood-submission/marble-tray-screen.test.tsx`
- `src/features/history/history-screen.test.tsx`
- `src/features/dashboard/manager-route.test.tsx`

What:
- Run the focused Jest command from `C:\MoodMarble\apps\mobile`.

Why:
- This checks that the dependency removal did not regress the key flows already covered by mobile unit tests.

How:
- Use the exact `pnpm exec jest --runTestsByPath ...` invocation from the prompt.
- Expect all four suites to pass.
- If the suite count or test count differs, report the actual result rather than forcing the expected count.

### 10. Commit only after all verification gates pass

Files eligible for staging:
- `apps/mobile/package.json`
- `pnpm-lock.yaml`
- `apps/mobile/detox.config.cjs` only if it remains part of the intended fix baseline at execution time

What:
- Create the requested atomic commit only after:
  - the manual E2E run passes both journeys, and
  - the targeted Jest regression command passes

Why:
- This preserves the user's requirement to commit only a verified build fix and to avoid pushing until approval.

How:
- Stage only the intended fix files.
- Use commit message:
  - `fix(e2e): remove unused @expo/ui canary, add reversePorts to detox config`
- Do not push.
- If `apps/mobile/detox.config.cjs` is unchanged relative to the chosen baseline when execution starts, do not introduce an unnecessary edit solely to satisfy the original prompt wording.

## Assumptions & Decisions

- The current worktree is the execution baseline, per user instruction.
- `apps/mobile/detox.config.cjs` already satisfies the `reversePorts` requirement, so no further config edit is currently planned.
- `apps/mobile/scripts/detox-build-android.cjs` remains out of scope even though it differs from the prompt's expected `subst M:` behavior.
- No files under `apps/mobile/src`, `apps/backend`, `packages/shared`, `apps/mobile/e2e`, or `apps/mobile/scripts/detox-build-android.cjs` will be edited as part of this fix.
- The crash hypothesis being acted on is the confirmed unused native autolinked package path through `@expo/ui`.
- If execution discovers any new `@expo/ui` import in source, the fix stops immediately and the dependency is not removed.

## Verification Steps

1. Confirm `@expo/ui` grep in `apps/mobile/src` returns zero files.
2. Confirm `apps/mobile/package.json` no longer contains `@expo/ui`.
3. Confirm `apps/mobile/detox.config.cjs` still contains `reversePorts: [8081, 8097]`.
4. Run `pnpm install` from `C:\MoodMarble` and verify a successful exit.
5. Remove `apps/mobile/android/app/build/outputs/apk` if present.
6. Run `pnpm e2e:android:build` from `C:\MoodMarble\apps\mobile` and verify both APK paths exist.
7. Optionally check the rebuilt APK with `aapt2` if available.
8. Run the targeted Jest regression command and record the actual suite/test counts.
9. Provide the manual three-terminal backend/Metro/Detox sequence exactly as specified.
10. After user-confirmed E2E success plus local regression success, create the requested commit and leave it unpushed.
